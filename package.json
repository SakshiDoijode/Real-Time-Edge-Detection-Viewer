{
  "name": "edge-detection-typescript",
  "version": "1.0.0",
  "description": "Real-time edge detection web application using TypeScript",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python -m http.server 8000",
    "dev": "tsc --watch & python -m http.server 8000",
    "clean": "rmdir /s /q dist 2>nul || rm -rf dist"
  },
  "keywords": [
    "edge-detection",
    "computer-vision",
    "typescript",
    "webcam",
    "real-time"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
